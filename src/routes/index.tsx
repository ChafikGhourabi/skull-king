// src/routes/index.tsx
// This route is handled entirely by the loader in src/router/index.tsx
// The loader always throws a redirect — this component is never rendered
export function Component() {
  return null
}
